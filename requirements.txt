fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.7
python-multipart>=0.0.9
PyJWT>=2.8
bcrypt>=4.1
slowapi>=0.1.9
limits>=3.13

# SQLite 内置于 Python 标准库，无需额外驱动。
# 如需执行一次性主数据迁移（scripts/migrate_pg_master_data.py），临时安装：
#   pip install psycopg2-binary
